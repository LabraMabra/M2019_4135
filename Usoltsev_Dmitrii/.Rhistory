iris
iris
subs <- function(data, row_selector, column_selector, split_selector)c{
sub <- subset(data, select = c(column_selector))[row_selector, ,drop=FALSE]
}
print(subs(iris,5:10, 2:5,'Species'))
subs <- function(data, row_selector, column_selector, split_selector)c{
sub <- subset(data, select = c(column_selector))[row_selector, ,drop=FALSE]
}
print(subs(iris,5:10, 2:5,'Species'))
subs <- function(data, row_selector, column_selector, split_selector) {
sub <- subset(data, select = c(column_selector))[row_selector, ,drop=FALSE]
}
print(subs(iris,5:10, 2:5,'Species'))
leftmost_segment
x <- runif(1000,0,1)
y <- runif(1000,0,1)
leftmost_segment <- pmin(x,y)
middle_segment <- pmax(x,y) -leftmost_segment
rightmost_segment <- 1- pmax(x,y)
hist(leftmost_segment,prob = T, xlab="length")
hist(middle_segment,prob = T, xlab="length")
hist(rightmost_segment ,prob = T, xlab="length")
result <- sum(x<y & x<0.5 & y<x+0.5 & y>0.5) | (y<x & y<0.5 & y>x-0.5 & y>0.5)/1000
hist(leftmost_segment,prob = T, xlab="length")
hist(middle_segment,prob = T, xlab="length")
hist(rightmost_segment ,prob = T, xlab="length")
x <- runif(1000,0,1)
y <- runif(1000,0,1)
leftmost_segment <- pmin(x,y)
middle_segment <- pmax(x,y) -leftmost_segment
rightmost_segment <- 1- pmax(x,y)
hist(leftmost_segment,prob = T, xlab="length")
hist(middle_segment,prob = T, xlab="length")
hist(rightmost_segment ,prob = T, xlab="length")
result <- sum(x<y & x<0.5 & y<x+0.5 & y>0.5) | (y<x & y<0.5 & y>x-0.5 & y>0.5)/1000
x <- runif(1000,0,1)
y <- runif(1000,0,1)
leftmost_segment <- pmin(x,y)
middle_segment <- pmax(x,y) -leftmost_segment
rightmost_segment <- 1- pmax(x,y)
hist(leftmost_segment,prob = T, xlab="length")
hist(middle_segment,prob = T, xlab="length")
hist(rightmost_segment ,prob = T, xlab="length")
result <- sum(x<y & x<0.5 & y<x+0.5 & y>0.5) | (y<x & y<0.5 & y>x-0.5 & y>0.5)/1000
hist(middle_segment,prob = T, xlab="length")
hist(rightmost_segment ,prob = T, xlab="length")
hist(leftmost_segment,prob = T, xlab="length")
hist(middle_segment,prob = T, xlab="length")
hist(rightmost_segment ,prob = T, xlab="length")
x <- runif(1000,0,1)
y <- runif(1000,0,1)
leftmost_segment <- pmin(x,y)
middle_segment <- pmax(x,y) -leftmost_segment
rightmost_segment <- 1- pmax(x,y)
hist(leftmost_segment,prob = T, xlab="length")
hist(middle_segment,prob = T, xlab="length")
hist(rightmost_segment ,prob = T, xlab="length")
result <- sum(x<y & x<0.5 & y<x+0.5 & y>0.5) | (y<x & y<0.5 & y>x-0.5 & y>0.5)/1000
x <- runif(1000,0,1)
y <- runif(1000,0,1)
leftmost_segment <- pmin(x,y)
middle_segment <- pmax(x,y) -leftmost_segment
rightmost_segment <- 1- pmax(x,y)
hist(leftmost_segment,prob = T, xlab="length")
hist(middle_segment,prob = T, xlab="length")
hist(rightmost_segment ,prob = T, xlab="length")
result <- sum(x<y & x<0.5 & y<x+0.5 & y>0.5) | (y<x & y<0.5 & y>x-0.5 & y>0.5)/1000
x <- runif(1000,0,1)
y <- runif(1000,0,1)
leftmost_segment <- pmin(x,y)
middle_segment <- pmax(x,y) -leftmost_segment
rightmost_segment <- 1- pmax(x,y)
hist(leftmost_segment,prob = T, xlab="length")
hist(middle_segment,prob = T, xlab="length")
hist(rightmost_segment ,prob = T, xlab="length")
result <- sum(x<y & x<0.5 & y<x+0.5 & y>0.5) | (y<x & y<0.5 & y>x-0.5 & y>0.5)/1000
hist(leftmost_segment,prob = T, xlab="length")
hist(middle_segment,prob = T, xlab="length")
hist(rightmost_segment ,prob = T, xlab="length")
hist(leftmost_segment,prob = T, xlab="length")
hist(middle_segment,prob = T, xlab="length")
subs <- function(data, row_selector, column_selector, split_selector) {
sub <- subset(data, select = c(column_selector))[row_selector, ,drop=FALSE]
x=sub[,split_selector]
print(x)
}
print(subs(iris,5:10, 2:5,'Species'))
subs <- function(data, row_selector, column_selector, split_selector) {
sub <- subset(data, select = c(column_selector))[row_selector, ,drop=FALSE]
x=sub[,split_selector,drop=FALSE]
print(x)
}
print(subs(iris,5:10, 2:5,'Species'))
subs <- function(data, row_selector, column_selector, split_selector) {
sub <- subset(data, select = c(column_selector))[row_selector, ,drop=FALSE]
x=sub[,split_selector,drop=FALSE]
}
print(subs(iris,5:10, 2:5,'Species'))
subs <- function(data, row_selector, column_selector, split_selector) {
sub <- subset(data, select = c(column_selector))[row_selector, ,drop=FALSE]
x=sub[,split_selector,drop=FALSE]
}
print(subs(iris,5:20, 2:5,'Species'))
subs <- function(data, row_selector, column_selector, split_selector) {
sub <- subset(data, select = c(column_selector))[row_selector, ,drop=FALSE]
x=sub[,split_selector,drop=FALSE]
}
print(subs(iris,1:30, 2:5,'Species'))
subs <- function(data, row_selector, column_selector, split_selector) {
sub <- subset(data, select = c(column_selector))[row_selector, ,drop=FALSE]
x=sub[,split_selector,drop=FALSE]
}
print(subs(iris,1:50, 2:5,'Species'))
subs <- function(data, row_selector, column_selector, split_selector) {
sub <- subset(data, select = c(column_selector))[row_selector, ,drop=FALSE]
x=sub[,split_selector,drop=FALSE]
}
print(subs(iris,1:100, 2:5,'Species'))
f <- function(x)
{
if (is.numeric(x)) {
g <- sum(x)}
else {
g <- summary(x)
}
return (g)
}
aapp <- function(xfull,row_criteria, col_criteria, split_criteria) # where split_criteria is set as 'desirable column in quotes'
{
xf <- subset(xfull, select = c(col_criteria))[row_criteria, ,drop=FALSE]
spcr <- as.factor(xf[,split_criteria])
ls_1 <- lapply(split(xf, spcr, drop =TRUE), function(x){sapply(x,f)})
ls <- list(xf, ls_1)
return (ls)
}
print(aapp(iris,5:10, 2:5,'Species'))
subs <- function(data, row_selector, column_selector, split_selector) {
sub <- subset(data, select = c(column_selector))[row_selector, ,drop=FALSE]
x=as.factor(sub[,split_selector,drop=FALSE])
subs <- function(data, row_selector, column_selector, split_selector) {
sub <- subset(data, select = c(column_selector))[row_selector, ,drop=FALSE]
x=as.factor(sub[,split_selector,drop=FALSE])
}
print(subs(iris,1:100, 2:5,'Species'))
print(subs(iris,1:100, 2:5,'Species'))
subs <- function(data, row_selector, column_selector, split_selector) {
sub <- subset(data, select = c(column_selector))[row_selector, ,drop=FALSE]
x=sub[,split_selector,drop=FALSE]
}
print(subs(iris,1:100, 2:5,'Species'))
subs <- function(data, row_selector, column_selector, split_selector) {
sub <- subset(data, select = c(column_selector))[row_selector, ,drop=FALSE]
x=sub[,split_selector,drop=FALSE]
}
print(subs(iris,1:100, 2:5,'Species'))
